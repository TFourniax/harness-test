name: Finalize v0.8 release source

on:
  push:
    branches:
      - agent/import-harness-telegram
    paths:
      - ".github/v08_release_finalize.py"
      - ".github/workflows/v08-release-finalize.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  finalize-v08:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply release test transition
        run: |
          set -euo pipefail
          python .github/v08_release_finalize.py
          rm -f .github/v08_release_finalize.py .github/workflows/v08-release-finalize.yml

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install and verify v0.8 direct source
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e '.[dev]'
          rm -rf src/*.egg-info
          python -m pytest -q
          python -m compileall -q src tests
          python -m ruff check --select E9,F63,F7,F82 src tests

      - name: Commit only if verified
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Finalize v0.8 direct release source"
          git push origin "HEAD:${GITHUB_REF_NAME}"
