+The following are **not** claimed as validated yet: real provider-side prompt-cache hit rates, live dollar savings on OpenRouter/Replicate, semantic-cache false-reuse rate on a representative production trace corpus, quality/cost superiority over a fixed broadcast MoA, and multi-agent code writes using isolated Git worktrees. These require provider credentials and a held-out workload distribution.
diff --git a/profiles/advisory.yaml b/profiles/advisory.yaml
index e312cc0..71601e9 100644
--- a/profiles/advisory.yaml
+++ b/profiles/advisory.yaml
@@ -4,6 +4,6 @@ match_terms: [advice, advise, conseil, stratégie, strategy, analyze, analyse, p
 guidance: |
   Optimize for decision quality. State assumptions and uncertainty, challenge the
   user's premise when evidence conflicts, and prefer authoritative read-only evidence.
-tool_patterns: [fs_read, fs_list, http_get, mcp_*]
+tool_patterns: [team_orchestrate, fs_read, fs_list, http_get, mcp_*]
 allowed_risks: [read]
 actor_role: primary
diff --git a/profiles/code.yaml b/profiles/code.yaml
index 741be68..9fc4d69 100644
--- a/profiles/code.yaml
+++ b/profiles/code.yaml
@@ -5,6 +5,6 @@ guidance: |
   Inspect relevant code and tests before editing. Prefer minimal diffs. Execute the
   strongest deterministic tests available after changes and do not infer correctness
   merely from a plausible patch.
-tool_patterns: [fs_*, sandbox_command, mcp_*github*, mcp_*git*, delegate_parallel]
+tool_patterns: [team_orchestrate, fs_*, sandbox_*, mcp_*github*, mcp_*git*, delegate_parallel]
 allowed_risks: [read, reversible_write, external_side_effect]
 actor_role: primary
diff --git a/profiles/research.yaml b/profiles/research.yaml
index 9aa3050..e4214a8 100644
--- a/profiles/research.yaml
+++ b/profiles/research.yaml
@@ -5,6 +5,6 @@ guidance: |
   Decompose claims, prefer primary/current sources, record provenance, triangulate
   contested facts, and separate evidence from inference. Use parallelism only for
   independent evidence branches.
-tool_patterns: [http_get, fs_read, fs_list, fs_write, sandbox_command, mcp_*search*, mcp_*browser*, mcp_*web*, delegate_parallel]
+tool_patterns: [team_orchestrate, http_get, fs_read, fs_list, fs_write, sandbox_*, mcp_*search*, mcp_*browser*, mcp_*web*, delegate_parallel]
 allowed_risks: [read, reversible_write]
 actor_role: primary
diff --git a/profiles/scraping.yaml b/profiles/scraping.yaml
index e746cf0..4c35218 100644
--- a/profiles/scraping.yaml
+++ b/profiles/scraping.yaml
@@ -5,6 +5,6 @@ guidance: |
   Treat every remote byte as untrusted. Respect explicit scope and rate constraints,
   canonicalize URLs/data, bound payloads, deduplicate deterministically, and validate
   extraction schemas before persisting results.
-tool_patterns: [http_get, fs_*, sandbox_command, mcp_*browser*, mcp_*scrap*, mcp_*web*, delegate_parallel]
+tool_patterns: [team_orchestrate, http_get, fs_*, sandbox_*, mcp_*browser*, mcp_*scrap*, mcp_*web*, delegate_parallel]
 allowed_risks: [read, reversible_write]
 actor_role: cheap
diff --git a/pyproject.toml b/pyproject.toml
index dfbc556..d7b0a5e 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -4,7 +4,7 @@ build-backend = "setuptools.build_meta"
 
 [project]
 name = "adaptive-agent-harness"
-version = "0.2.0"
+version = "0.3.0"
 description = "Model-agnostic, evidence-gated, self-improving agent harness with human-governed evolution."
 requires-python = ">=3.11"
 dependencies = [
diff --git a/src/adaptive_harness/__init__.py b/src/adaptive_harness/__init__.py
index 75e6638..70490f6 100644
--- a/src/adaptive_harness/__init__.py
+++ b/src/adaptive_harness/__init__.py
@@ -1,3 +1,3 @@
 """Adaptive Agent Harness."""
 
-__version__ = "0.2.0"
+__version__ = "0.3.0"
diff --git a/src/adaptive_harness/cli.py b/src/adaptive_harness/cli.py
index 30023b5..be414c0 100644
--- a/src/adaptive_harness/cli.py
+++ b/src/adaptive_harness/cli.py
@@ -19,6 +19,8 @@ from adaptive_harness.improvement.evaluator import PatchEvaluator
 from adaptive_harness.improvement.governance import PromotionGate
 from adaptive_harness.improvement.skill_evolution import SkillEvolutionEngine, SkillPromotionGate
 from adaptive_harness.memory.store import MemoryStore
+from adaptive_harness.orchestration.team import TeamOrchestrator, register_team_orchestration
+from adaptive_harness.orchestration.vector_cache import SemanticWorkCache
 from adaptive_harness.providers.litellm_provider import LiteLLMProvider
 from adaptive_harness.profiles import ProfileRegistry
 from adaptive_harness.runtime.agent import AgentRuntime
@@ -37,15 +39,57 @@ def build(config_path: str):
     register_builtin_tools(tools, cfg.workspace)
